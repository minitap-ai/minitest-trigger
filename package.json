{
  "name": "minitest-trigger",
  "version": "1.0.0",
  "description": "GitHub Action for triggering your Minitest suite from CI workflows",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "bundle": "ncc build src/main.ts --source-map --license licenses.txt",
    "lint": "eslint src/",
    "test": "vitest run",
    "format": "prettier --write 'src/**/*.ts'",
    "format:check": "prettier --check 'src/**/*.ts'",
    "all": "npm run build && npm run lint && npm run format:check && npm run bundle && npm run verify:bundle",
    "verify:bundle": "node scripts/verify-bundle.mjs"
  },
  "keywords": [
    "github-action",
    "minitap",
    "minitest",
    "testing",
    "mobile-testing",
    "ci-cd"
  ],
  "author": "Minitap",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/http-client": "^2.2.3"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.1",
    "@types/node": "^22.15.2",
    "@vercel/ncc": "^0.45.0",
    "eslint": "^9.25.1",
    "prettier": "^3.5.3",
    "typescript": "^7.0.0",
    "typescript-eslint": "^8.31.0",
    "vitest": "^4.1.11"
  }
}
